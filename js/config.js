window.PORTFOLIO_CONFIG = {
  // Optional ArcGIS API key. Leave empty to use public Esri canvas tiles.
  // To keep a real key out of git, copy this file to config.local.js and
  // load that file instead — config.local.js is gitignored.
  arcgisApiKey: ""
};
